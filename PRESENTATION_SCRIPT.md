# 미니 지피티 4분 발표 대본

## 0:00-0:25 시작

안녕하세요. 4조 발표 시작하겠습니다

먼저 저희 발표의 핵심은 

직접 구현한 미니 지피티가 NSMC 데이터에서 실제로 학습됐는지, 그리고 그 결과를 손실값과 그래프로 설명할 수 있는지입니다.

그래서 그 과정의 결과를 중심으로 보겠습니다. 
사전학습 손실값이 어떻게 줄었는지, 
드롭아웃과 활성화 함수 선택이 결과에 어떻게 나타났는지, 
마지막으로 감성분류 파인튜닝에서 어느 정도 성능이 나왔는지를 보겠습니다.

## 0:25-1:20 베이직 사전학습 결과
보이는 세팅은 저희 팀에서 베이직 모델로 선택한 사전학습결과 입니다

베이직 모델은 최고 성능을 찾기 위한 모델이라기보다는, 
전체 학습 파이프라인이 정상적으로 도는지 확인하기 위한 기준 모델로 잡았습니다.
위 설정으로 이어서 학습한 최고 성능 체크포인트는 1392 에폭에서 검증 손실값 4.5587을 기록했습니다.

## 1:20-2:10 수렴성 확인

다음은 학습이 제대로 진행됐는지와 수렴성입니다.

그래프 위쪽은 100 에폭 구간별 평균 학습 손실값과 검증 손실값입니다. 학습 손실값 평균은 4.6624에서 3.9448까지 줄었고, 검증 손실값 평균도 4.8953에서 4.5625까지 줄었습니다.

그래프 아래쪽은 직전 100 에폭 구간과 비교해서 손실값이 얼마나 더 줄었는지를 보여줍니다. 마지막 구간에서는 학습 손실값 감소량이 0.0095, 검증 손실값 감소량이 0.0028까지 작아졌습니다.

그래서 이 그래프에서 확인할수있는건  학습 손실값과 검증 손실값이 모두 낮아졌기 때문에 학습은 실제로 진행됐고, 뒤로 갈수록 감소폭이 작아졌기 때문에 수렴에 가까워지는 양상을 보인다는것입니다.

추가로 최종 파라미터 후보도 따로 학습했습니다. 베이스는 1392 에폭까지 학습했고, 후보 조합은 250 에폭까지만 학습했습니다. 후보 조합은 225 에폭에서 검증 손실값 1.6420을 기록했고, 생성 결과에서도 영화 리뷰 문장 형태가 출력되는 것을 확인했습니다.


마지막으로 저희가 파라미터를 일일이 실험할떄 어떤 근거로 채택했는지 소개 하겠습니다
우선 이런 파라미터를 고를 때 짧은 실험이 어느 정도 믿을 만한지도 확인했습니다. 
대표적으로 드롭아웃 실험에서 50 에폭 기준으로 드롭아웃 0.0의 검증 손실값이 불안정해지는 흐름을 봤고, 
이 판단이 맞는지 확인하려고 같은 조건을 500 에폭까지 늘렸습니다. 그 결과 드롭아웃 0.0은 초반 이후 검증 손실값이 다시 올라갔고, 0.1과 0.3은 계속 낮아지는 흐름을 유지했습니다. 
그래서 50 에폭 정도의 짧은 실험에서도 과적합 경향을 어느 정도 포착할 수 있겠다고 판단했습니다.
그래서 이 이후 모든 실험은 50 에폭 단위로 진행하여 위에서 보신 최종 파라미터 를 선택하게되었습니다
## 2:10-3:10 겔루 선택 근거

다음은 저희 메인 실험 주제인 활성화 함수 선택입니다.

저희가 배운 시그모이드, 렐루, 겔루 세 함수 중에서 왜 지피티의 FFN에는 겔루가 더 적합한지 실험으로 확인했습니다.

먼저 시그모이드입니다. 시그모이드는 입력 절댓값이 커질수록 미분값이 작아지는 특징이 있습니다. 
그래서 실제로 에프에프엔 첫 번째 리니어 레이어의 가중치 업데이트 신호, 
디엘 디더블유의 절댓값이 얼마나 작아지는지 확인했습니다.

그래프에서 가로축 퍼센트는 해당 업데이트 신호 크기 구간에 속한 가중치 비율입니다.

그래프에서 봐야 할 부분은 분포가 어디에 몰려 있는지입니다. 
시그모이드는 0에 가까운 작은 값 구간에 비율이 더 많이 몰려 있고, 겔루는 상대적으로 더 넓은 구간에 퍼져 있습니다.

즉 시그모이드를 쓰면 에프에프엔 가중치로 전달되는 업데이트 신호가 작게 몰리는 경향이 있었고, 겔루는 그 신호를 더 넓게 유지했습니다.

업데이트 신호가 작게 몰리면 많은 가중치가 거의 변하지 않기 때문에, 모델이 새로운 패턴을 배우는 속도와 표현력이 떨어질 수 있습니다.

렐루는 다른 문제가 있습니다. 렐루는 입력이 0보다 작으면 미분값이 0이 되기 때문에, 역전파에서 학습 신호가 끊깁니다.
그래프는 이 미분값이 정확히 0이 된 비율을 보여줍니다. 우리 실험에서 렐루는 약 45% 정도가 0으로 끊겼고, 겔루는 거의 0%였습니다.

반면 겔루는 음수 입력을 완전히 0으로 자르지 않고 부드럽게 줄입니다. 그래서 시그모이드처럼 업데이트 신호가 작게 몰리는 문제도 줄이고, 
렐루처럼 역전파 신호가 끊기는 문제도 피할 수 있다고 해석했습니다.

## 3:10-3:40 파인튜닝 결과

마지막은 감성분류 파인튜닝 결과입니다. 이 부분은 메인 실험이라기보다는, 구현한 사전학습 모델을 분류 문제에도 연결해 본 결과입니다.

파라미터는 화면에 보이는바와 같습니다

저희의 1392 에폭시 사전학습 체크포인트를 불러오고, 그 위에 감성분류 헤드를 붙여 전체 라벨 데이터로 학습했습니다.

조기 종료 기준은 검증 정확도가 20 에폭 동안 개선되지 않는 경우로 잡았습니다. 학습은 49 에폭에서 멈췄고, 가장 좋았던 모델은 29 에폭 체크포인트였습니다. 전체 테스트 데이터 기준 정확도는 0.8517이 나왔습니다.

## 3:40-4:00 결론

정리하면, 저희는 이번 실험 과정을 통해 왜 시그모이드나 렐루가 아니라 겔루를 쓰는지 어느 정도 입증했다고 생각합니다.

시그모이드는 업데이트 신호가 작은 값에 몰리는 경향이 있었고, 렐루는 음수 구간에서 역전파 신호가 끊기는 문제가 있었습니다. 겔루는 이 두 문제를 모두 완화하는 방향으로 동작했습니다.

아쉬운 점은 시간상 모든 파라미터 조합을 전부 테스트하고, 그 경향성을 모두 그래프로 확인하지는 못했다는 점입니다.
